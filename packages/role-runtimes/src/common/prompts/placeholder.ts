/**
 * Placeholder bundled prompt — used by `integration_test.ts` only.
 *
 * Step 09 (engineer specialisation) will export a `engineer` prompt under
 * `packages/role-runtimes/src/engineer/prompt.ts`; step 18 will export
 * the four PO prompts under `packages/role-runtimes/src/po/prompts/`.
 * This file is not used by any production cycle — the cycle's
 * `promptResolver` parameter is supplied by the role.
 *
 * @module
 */

export const PLACEHOLDER_PROMPT_BODY =
  "PLACEHOLDER PROMPT — used by the role-runtime-common integration test only. Replace per role in steps 09 / 18.\n";

export const PLACEHOLDER_PROMPT_NAME = "placeholder";
