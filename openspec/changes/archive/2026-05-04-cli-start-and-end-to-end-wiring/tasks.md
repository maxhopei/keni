## 1. Wire shapes and shared constants

- [x] 1.1 Add `HealthResponse` and `HealthEnvelope` to `packages/shared/src/wire/health.ts` per the `orchestration-server` capability's `/health` requirement (`{ status: "ok", project_id, uptime_ms, version }` body, success-envelope shape with `project_id`). Re-export both types from `packages/shared/src/wire/mod.ts`. Add a corresponding zod schema to `packages/server/src/wire/health.ts` declared with the `z.ZodType<HealthResponse>` constraint per the existing wire-shape convention; add the type-equivalence assertion in `packages/server/src/wire/health_test.ts`.
- [x] 1.2 Add the build-time `VERSION` constant to `packages/shared/src/version.ts` (`export const VERSION = "0.0.0-prototype" as const;`) and re-export it from `packages/shared/src/mod.ts`. Document inline that future binary packaging will replace it via a build arg.
- [x] 1.3 Add `REST_PREFIXES` to `packages/server/src/createServer.ts` (or a new `packages/server/src/restPrefixes.ts` module) as the closed `readonly ["/agents", "/tickets", "/prs", "/activity", "/health", "/events"]` and re-export from the package's main barrel (`packages/server/src/main.ts`). Add a unit test that asserts the value is the documented closed list and that every existing route group's prefix is a member.

## 2. Orchestration server: `/health` route + middleware-order carve-out

- [x] 2.1 Create `packages/server/src/routes/health.ts` exporting a Hono handler that returns `200 { data: { status: "ok", project_id, uptime_ms, version }, project_id }`. The handler reads `serverStartedAt` from `c.var` (the `createServer` deps wiring exposes it). When `serverStartedAt` is absent, `uptime_ms` is `0`.
- [x] 2.2 Update `packages/server/src/createServer.ts` to extend `ServerDeps` with `serverStartedAt?: Date`, register the `/health` route handler **before** the `roleIdentity` middleware (the carve-out per the modified middleware-order requirement), and continue to register the existing routes after `roleIdentity`. Update the existing `createServer_test.ts` middleware-order test to assert the new order: `["requestId", "requestLog", /* /health carve-out */, "roleIdentity", /* routes */]`. Add a new test asserting `GET /health` succeeds without `X-Keni-Role` and that the captured request-log line for `/health` has `role: null` and `agent: null`.
- [x] 2.3 Add `packages/server/src/routes/health_test.ts` covering the seven `/health` scenarios in the orchestration-server delta: no-role-header success, with-role-header success, `uptime_ms` advances over time, `serverStartedAt` absent → `uptime_ms: 0`, no store mutation / no bus emit, `POST /health` returns 4xx, request-log line carries `role: null` / `agent: null`. Use an in-memory `agentRuntimeStateStore`, an instrumented `eventBus` (`captureBusBuffer`), and `captureLogSink(buffer)` for the request-log assertion.
- [x] 2.4 Update `packages/server/src/runServer.ts` to capture `serverStartedAt: new Date()` after `Deno.serve`'s `onListen` fires (i.e., immediately after `startServer` resolves) and pass it to the Hono app via the existing `ServerDeps` route — note that `createServer` is called by `startServer`, so wire the field through `StartServerOptions` (extend `StartServerOptions` and forward verbatim into the `createServer` call inside `startServer.ts`). Update `runServer_test.ts` to assert `GET /health` returns the documented envelope after a successful boot.

## 3. Orchestration server: static SPA route group

- [x] 3.1 Create `packages/server/src/routes/static.ts` exporting (a) a `mountStaticSpa(app, { staticAssetsRoot })` function that registers the `/`, `/assets/*`, and SPA-fallthrough handlers per the `orchestration-server` delta; (b) a `StaticAssetsRootInvalid` typed error class; (c) a `validateStaticAssetsRoot(root: string)` helper that throws `StaticAssetsRootInvalid` when the path does not exist or does not contain `index.html`. The fallthrough handler MUST consult `REST_PREFIXES` to decide whether to fall through (the fallthrough applies ONLY to `GET` requests whose path does not start with any allowlisted prefix).
- [x] 3.2 Add immutable cache headers to `/assets/*` responses (`Cache-Control: public, max-age=31536000, immutable`). Use `@std/path/extname` to set the `Content-Type` header (`.js → text/javascript`, `.css → text/css`, `.svg → image/svg+xml`, `.html → text/html`, `.json → application/json`, default `application/octet-stream`).
- [x] 3.3 Add path-traversal protection: every requested path under `/assets/*` MUST be resolved against `staticAssetsRoot` and the resolved absolute path MUST start with `staticAssetsRoot + "/"` (or equal `staticAssetsRoot`); otherwise return `404`. Test this with `GET /assets/../../etc/passwd` (the request MAY be URL-normalised by Hono, but the handler must defend against the worst case).
- [x] 3.4 Update `packages/server/src/createServer.ts` to extend `ServerDeps` with `staticAssetsRoot?: string` and to call `validateStaticAssetsRoot(staticAssetsRoot)` synchronously (the throw becomes the `createServer` exception) followed by `mountStaticSpa(app, { staticAssetsRoot })` AFTER all REST route groups are mounted (so REST endpoints win the route ordering).
- [x] 3.5 Add `packages/server/src/routes/static_test.ts` covering the eight static-route scenarios in the delta: `GET /` serves index.html, `GET /assets/<file>` serves with the immutable cache header, `GET /tickets/ticket-0001` runs the REST handler (REST wins over SPA fallthrough), `GET /some/spa/route` falls through to index.html, `POST /some/spa/route` returns 404, path traversal returns 404, `staticAssetsRoot` absent → `GET /` is 404, `staticAssetsRoot` invalid → `createServer` throws `StaticAssetsRootInvalid`. Use a temp-dir fixture with a stub `index.html` and a stub `assets/main-abc123.js`.

## 4. Orchestration server: `state.json` paused-agents persistence

- [x] 4.1 Extend `packages/server/src/createServer.ts` `ServerDeps` with `pausedAgentsPersister?: (paused: readonly string[]) => Promise<void>`. The field is OPTIONAL — every existing `createServer({...}, opts)` call site keeps working unchanged.
- [x] 4.2 Update the existing pause/resume route handlers in `packages/server/src/routes/agents.ts` to call `pausedAgentsPersister(agentRuntimeStateStore.list().filter(a => a.paused).map(a => a.id))` AFTER the `agent.state_changed` emit, only when the call actually flipped (`changed: true`). Wrap in `try/catch`: a rejection logs at warn level via `LogSink` and DOES NOT fail the request. When `pausedAgentsPersister` is absent, skip the call silently.
- [x] 4.3 Update the existing `agents_test.ts` to add three new cases: persister supplied → write happens after `agent.state_changed`; persister rejection → request still 200 + warn-level log line emitted; persister absent → no extra log line.

## 5. CLI: scaffold `start/` package directory and dispatcher arm

- [x] 5.1 Create `packages/cli/src/start/mod.ts` exporting `runStart(args, io)` and `parseStartArgs(rest)`. `runStart` is the public entry point (called by the dispatcher); `parseStartArgs` is the typed argv parser (separated for testability). Both use the existing `DispatcherIO` shape from `packages/cli/src/main.ts`.
- [x] 5.2 Update `packages/cli/src/main.ts`'s dispatcher to add the `case "start":` arm: `case "start": return await runStart(parseStartArgs(rest), { out, err });`. The arm mirrors the existing `case "init":` shape verbatim. Update the existing help text (`formatHelp()` in `messages.ts`) to list `keni start [path]` alongside `keni init [path]`.
- [x] 5.3 Add `packages/cli/src/main_test.ts` cases: `runDispatcher(["start"])` calls `runStart` with the parsed argv and surfaces its exit code; `runDispatcher(["--help"])` lists `start` in the help text; `runDispatcher(["start", "--unknown-flag"])` returns 2 with the usage error.

## 6. CLI: argv parsing for `keni start`

- [x] 6.1 Create `packages/cli/src/start/args.ts` exporting `parseStartArgs(rest: readonly string[]): ParsedStartArgs`. The parser handles the documented flags (`--project`, `--port`, `--port-range`, `--host`, `--spa-dev-url`, `--spa-bundle`, `--shutdown-grace-ms`) plus the OPTIONAL positional argument. Positional wins over `--project` (with a warn-level log via the IO writer); malformed `--port` / `--port-range` / `--shutdown-grace-ms` values throw `UsageError` (which the dispatcher surfaces as exit 2).
- [x] 6.2 Create `packages/cli/src/start/args_test.ts` covering the eight `cli-start` argv scenarios in the spec: positional default, positional override, positional+flag (positional wins), unknown flag rejected, missing project file (handled by `runStart` not `parseStartArgs` — this test asserts the parsed args carry the path; the missing-file check is in task 8), `--port-range 7777` (malformed) rejected, `--port 8080` collapses range, `--port-range 9000-9005` replaces range.

## 7. CLI: layered config loader

- [x] 7.1 Create `packages/cli/src/start/loadConfig.ts` exporting `loadKeniConfig({ projectDir, homeDir, env })` returning `{ projectConfig: ProjectConfig, startConfig: KeniStartConfig }`. Read `~/.keni/config.yaml` (when present), read `<projectDir>/.keni/project.yaml` (REQUIRED — throw `ProjectStateError` from the existing `init/errors.ts` family on absence), shallow-merge top-level keys (project wins). Apply the documented defaults (`port_range: { start: 7777, end: 7787 }`, `host: "127.0.0.1"`, `shutdown_grace_ms: 2_000`, `spa.mode: "bundled"`).
- [x] 7.2 Apply the CLI flag overrides on top of the merged config: `--port n` → `port_range: { start: n, end: n }`; `--port-range a-b` → `port_range: { start: a, end: b }`; `--host h` → `host: h`; `--spa-dev-url url` → `spa.mode: "dev"`. The flag-override step is a separate function (`applyFlagOverrides(merged, flags)`) to keep the merge pure.
- [x] 7.3 Create `packages/cli/src/start/loadConfig_test.ts` covering the seven layered-config scenarios in the `cli-start` spec: project wins, global wins when project absent, defaults apply when neither specifies, `--port` collapses range, `--port-range` replaces range, top-level keys are replaced not merged, `~/.keni/config.yaml` absent treated as `{}`.

## 8. CLI: `.env` loader

- [x] 8.1 Create `packages/cli/src/start/loadEnv.ts` exporting `loadEnvFile({ projectDir, logSink? })` returning `Record<string, string>`. Parse `<projectDir>/.env` (when present) per the documented regex (`^([A-Za-z_][A-Za-z0-9_]*)=(.*)$`); strip surrounding double quotes; ignore blank lines and `#`-prefix lines; warn-log + skip malformed lines. Return `{}` when the file is absent (no warn). Total parser size target: ≤50 lines (the spec's "minimal" promise).
- [x] 8.2 Add `applyEnvOverlay(parsed: Record<string, string>, env: { get: ..., set: ... })` that calls `env.set(key, value)` only when `env.get(key) === undefined` (the calling shell wins). The default `env` argument is `{ get: Deno.env.get.bind(Deno.env), set: Deno.env.set.bind(Deno.env) }`; tests pass an in-memory `Map`-backed env stub.
- [x] 8.3 Create `packages/cli/src/start/loadEnv_test.ts` covering the seven `.env` scenarios in the `cli-start` spec: `.env` provides defaults, calling shell wins, comments and blank lines ignored, double-quoted values stripped, malformed lines warned + skipped, `.env` absent is no-op, multiline / interpolation NOT supported.

## 9. CLI: port-range walker

- [x] 9.1 Create `packages/cli/src/start/port.ts` exporting `bindPortInRange({ startServer, host, range, logSink })` that iterates `range.start..range.end` (inclusive), calls `startServer({ host, port })` for each, and on `Deno.errors.AddrInUse` logs a warn line and tries the next port. Returns the resolved `StartedServer` handle on success; throws `PortRangeExhaustedError` when every port is busy; rethrows non-`AddrInUse` errors verbatim. The function is pure with respect to the injected `startServer` (tests inject a stub).
- [x] 9.2 Wire `bindPortInRange` into `runStart` so the resolved port range from task 7 walks the binder. When `range.start === range.end` (the `--port n` case), the binder still walks (one-element range) but `PortRangeExhaustedError`'s message names the single pinned port (per the spec's "no fallback" rule).
- [x] 9.3 Create `packages/cli/src/start/port_test.ts` covering the four port-range scenarios in the `cli-start` spec: walks past busy first port, range exhaustion returns exit 1 (via `PortRangeExhaustedError`), `--port n` does not retry on busy, non-`EADDRINUSE` errors do not retry. Use an injected fake `startServer` that records its calls and rejects with `Deno.errors.AddrInUse` or `Deno.errors.PermissionDenied` per the test case.

## 10. CLI: SPA-bundle resolution

- [x] 10.1 Create `packages/cli/src/start/spaBundle.ts` exporting `resolveSpaBundle({ flags, repoRoot? }): { mode: "bundled", root: string } | { mode: "dev", devUrl: string }`. Resolution order per the `cli-start` spec: explicit `--spa-bundle <path>` flag wins; otherwise the workspace-relative `<repoRoot>/packages/spa/dist/`; future binary-packaged paths (post-MVP — out of scope, document inline). When `--spa-dev-url <url>` is supplied, return `{ mode: "dev", devUrl: url }`. When `mode: "bundled"` and the resolved `dist/index.html` does not exist, throw `SpaBundleMissingError` whose message names the expected path and the `deno task build` invocation.
- [x] 10.2 Wire `resolveSpaBundle` into `runStart` so the resolved bundle path is passed to `runServer` via `RunServerDeps.staticAssetsRoot` (when `mode: "bundled"`); when `mode: "dev"`, skip the field. Print the dev-URL line per the spec's "Dev-mode startup prints the SPA dev URL line" scenario.
- [x] 10.3 Create `packages/cli/src/start/spaBundle_test.ts` covering the four SPA-bundle scenarios: production-mode resolves workspace-relative `dist/`, missing `dist/` fails with `SpaBundleMissingError`, `--spa-dev-url` returns dev-mode descriptor, `--spa-bundle` overrides the workspace resolution.

## 11. CLI: `state.json` paused-agents reader and persister

- [x] 11.1 Create `packages/cli/src/start/pausedAgents.ts` exporting (a) `readPausedAgents({ projectDir, roster, logSink }): readonly string[]` that reads `<projectDir>/.keni/state.json`, parses the OPTIONAL `paused_agents: string[]` array, drops entries not in the roster (with a warn-level log per dropped entry), returns `[]` for absent / malformed values (with a warn for malformed); (b) `persistPausedAgents({ projectDir, paused, logSink }): Promise<void>` that reads the existing `state.json`, replaces only the `paused_agents` key (preserving every other top-level key), writes back atomically (write-temp-then-rename to avoid torn writes). The persister returns a rejected promise on filesystem failure (the orchestration server's pause handler catches and warn-logs per the modified `orchestration-server` requirement).
- [x] 11.2 Wire `readPausedAgents` into `runStart` to populate `RunServerDeps.initiallyPausedAgents` before invoking `runServer`. Wire `persistPausedAgents` into `runStart` as the `pausedAgentsPersister` field on `RunServerDeps`'s `createServer` deps bag — the existing `runServer.ts` already builds the `ServerDeps` for `createServer`; this task extends that bag with the new field. Note that this requires extending `RunServerDeps` itself with `initiallyPausedAgents?: readonly string[]` and `pausedAgentsPersister?: (paused: readonly string[]) => Promise<void>` so tests can inject test doubles.
- [x] 11.3 Update `packages/server/src/runServer.ts` to (a) accept the two new optional fields on `RunServerDeps`; (b) thread `initiallyPausedAgents` through to `createInMemoryAgentRuntimeStateStore(roster, { initiallyPaused })` (extend the factory's signature accordingly); (c) thread `pausedAgentsPersister` through to `createServer` via `ServerDeps`. Update `agentState.ts`'s `createInMemoryAgentRuntimeStateStore` to accept the new optional `{ initiallyPaused: readonly string[] }` and seed `paused: true` for those entries.
- [x] 11.4 Create `packages/cli/src/start/pausedAgents_test.ts` covering the six `state.json` scenarios in the `cli-start` spec: seeds the runtime store at boot, absence is no-op, unknown ids dropped with warn, malformed treated as `[]`, pause persists to `state.json`, write failure logs warn but does not fail. Use a temp-dir fixture with a `keni init`-style `state.json`.

## 12. CLI: graceful-shutdown sequence

- [x] 12.1 Create `packages/cli/src/start/shutdown.ts` exporting `runShutdownSequence({ scheduler, runtimeStore, serverHandle, graceMs, secondSignal, logSink }): Promise<number>` per the `cli-start` capability's documented sequence. Implementation order is exactly: `await scheduler.stop()`; iterate `runtimeStore.list()`, `await scheduler.interrupt(id)` IN SERIES for every `status === "running"` entry (catch and warn-log rejections); `await Promise.race([sleep(graceMs), waitForAbort(secondSignal)])`; `await serverHandle.abort()`; return `0`. When `secondSignal.aborted` becomes true at any step, short-circuit to return `130` (skip remaining steps including `serverHandle.abort()` per the spec).
- [x] 12.2 Add `installSignalHandlers(secondSignal: AbortController, onFirstSignal: () => void)` that registers SIGINT and SIGTERM listeners. The first signal calls `onFirstSignal()` exactly once; the second signal aborts `secondSignal`. Both handlers are removed when the function's returned cleanup callback fires (so `runStart`'s resolve does not leak handlers).
- [x] 12.3 Wire `installSignalHandlers` and `runShutdownSequence` into `runStart`. The shutdown grace is clamped at 10_000 ms per the spec's hard-cap rule (with a warn-level log line naming the configured value, the cap, and the clamped value when the cap fires).
- [x] 12.4 Create `packages/cli/src/start/shutdown_test.ts` covering the seven shutdown scenarios in the `cli-start` spec: first signal runs full sequence + exit 0, series interrupt (no overlap), second signal short-circuits to 130, SIGTERM behaves like SIGINT, hard-cap at 10_000 ms, scheduler.interrupt rejection does not block next agent, signal listeners removed on resolve. Use stubbed `scheduler`, `runtimeStore`, and `serverHandle` deps that record their call order and timestamps.

## 13. CLI: integration — `runStart` end-to-end

- [x] 13.1 Implement `packages/cli/src/start/mod.ts`'s `runStart(parsedArgs, io)` to compose the modules in order: (1) `loadKeniConfig` (throws `ProjectStateError` on missing `project.yaml`); (2) `applyFlagOverrides`; (3) `loadEnvFile` + `applyEnvOverlay`; (4) `resolveSpaBundle` (throws `SpaBundleMissingError` in production mode); (5) `readPausedAgents`; (6) build `RunServerDeps` (with `staticAssetsRoot`, `initiallyPausedAgents`, `pausedAgentsPersister`, the IO writers, the `LogSink`, the `bindPortInRange`-driven port resolution); (7) build the second-signal `AbortController`; (8) install signal handlers; (9) call `runServer(synthesised argv, deps)`; (10) on resolve, run the cleanup callback (remove signal handlers); (11) return the exit code.
- [x] 13.2 The synthesised argv passed to `runServer` carries the resolved port (the chosen one from `bindPortInRange`), the resolved host, and the project path. The port-range walking happens inside `runStart`, NOT inside `runServer`; once a port is bound, `runServer` accepts the port verbatim. This requires a small refactor: `runStart` calls `bindPortInRange` itself (passing a closure that builds `runServer`-style deps and calls `startServer` directly via the existing `@keni/server` export), then constructs the rest of the bootstrap (scheduler, engineer wiring) inline OR exposes `runServer`'s bootstrap-after-listen as a separate function. Pick the smaller refactor: add a new `RunServerDeps.startServerOverride?: (deps, opts) => Promise<StartedServer>` field that `runStart` populates with the `bindPortInRange`-wrapped `startServer`; `runServer` calls the override when present.
- [x] 13.3 Map all four exit codes per the `cli-start` spec's table: 0 (clean shutdown), 1 (filesystem / project-state failure — caught from `ProjectStateError`, `SpaBundleMissingError`, `PortRangeExhaustedError`, etc.), 2 (`UsageError` from argv parsing), 130 (forced shutdown — returned by `runShutdownSequence` when the second signal fires).
- [x] 13.4 Add the documented stdout startup line in production mode (`Keni server running at http://<host>:<port>\n`) and the second dev-mode line (`SPA dev mode — proxy your Vite dev server to <url>`) in dev mode. The lines go to `io.out`, not `console.log` directly, so tests can capture them.

## 14. CLI: end-to-end smoke test

- [x] 14.1 Create `packages/cli/src/start/start_e2e_test.ts` per the `cli-start` capability's smoke-test requirement. The test boots `runStart` against a temp-dir fixture (built by an in-test `runInit` from `packages/cli/src/init/mod.ts` plus a stub SPA `dist/` directory containing `index.html` with `id="root"` and a stub `assets/main-stub.js`). The test injects `RunStartDeps`: a `makeEngineerRunner` stub returning a precheck-skip runner, an `AbortSignal` for shutdown, capturing IO writers, and instrumented `scheduler` / `serverHandle` deps that record their call order.
- [x] 14.2 Assertions: stdout's startup line matches `Keni server running at http://127.0.0.1:\d+`; `GET /health` against the printed URL returns 200 with the documented envelope; `GET /` returns 200 with `Content-Type: text/html` and the body contains `id="root"`; the abort signal triggers the documented shutdown order (`scheduler.stop` → `serverHandle.abort`); the resolved exit code is 0.
- [x] 14.3 Cleanup: the temp-dir fixture is removed in a `try/finally`. The test does not require network access beyond `127.0.0.1`. The test respects the existing `developer-setup` "every command requires no network beyond initial install" rule.

## 15. README updates

- [x] 15.1 Restructure the existing "Run the orchestration server" subsection in `README.md` per the `developer-setup` delta: lead paragraph names `keni start` as the user-facing entry point with a cross-link; preserve the existing direct-`deno run` invocation under a new "Direct invocation (development)" sub-subsection; add one sentence naming `GET /health` and its role-header exemption with a cross-link to the `orchestration-server` capability spec.
- [x] 15.2 Add a new "Quickstart with `keni start`" subsection inside the "Getting started" section per the `developer-setup` delta. Place it after "Initialise a Keni project (`keni init`)" and before "Run the orchestration server". Cover the ten documented points (prerequisite, build prerequisite, invocation, expected stdout, next step, `.env` seam, layered-config seam, port range, shutdown contract, capability cross-link).
- [x] 15.3 Add the new top-level "End-to-end smoke test" section per the `developer-setup` delta. Place it as a peer of "Conventions" and "Repository layout". Cover the four-step user runbook, the prerequisites paragraph, and the cross-link to `packages/cli/src/start/start_e2e_test.ts` with the explicit "manual runbook vs. automated test" distinction.
- [x] 15.4 Update the existing "Initialise a Keni project (`keni init`)" subsection's closing sentence to cross-link forward to "Quickstart with `keni start`" so a reader following the README top-to-bottom is guided directly into the boot step.

## 16. Verification

- [x] 16.1 Run `deno task fmt` (rewriting any newly-touched files), `deno task fmt:check`, `deno task lint`, and `deno task check` from the workspace root. All four MUST exit 0.
- [x] 16.2 Run `deno task test` from the workspace root. The new tests in `packages/cli/src/start/`, `packages/server/src/routes/health_test.ts`, and `packages/server/src/routes/static_test.ts` MUST pass alongside every existing test.
- [x] 16.3 Run `deno task build` from the workspace root to ensure the SPA's Vite build still succeeds with the new code paths.
- [ ] 16.4 Manual end-to-end verification per the README's new "End-to-end smoke test" runbook: `keni init` an empty folder, `deno task build` (in the workspace root for the SPA bundle), `deno run -A packages/cli/src/main.ts start <empty-folder>`, open the printed URL in a browser, create a ticket via the SPA's "New ticket" form, observe the engineer drive it through `in_progress → … → ready_for_test`, fire SIGINT, observe the documented graceful shutdown.
- [ ] 16.5 Open the change for archive — once the `start_e2e_test` and the four green workspace tasks pass and the manual runbook confirms the user-visible flow, mark this change ready for `/opsx-archive`.
