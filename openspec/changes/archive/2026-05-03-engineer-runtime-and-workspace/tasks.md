## 1. Wire types and error / event additions

- [x] 1.1 Add `merge_conflict` to the `ErrorCode` enum in `packages/shared/src/wire/errors.ts` and update its zod schema; add a unit test asserting the new value is exported and the closed enum is still exhaustive.
- [x] 1.2 Add `pr_merged` to the `EventName` union/zod enum in `packages/shared/src/wire/activity.ts`; add a unit test asserting the new value is exported and the union is still exhaustive. _(Implementation note: activity-log `event` is an open string field, not a closed enum. Added `PR_MERGED_ACTIVITY_EVENT = "pr_merged"` as a re-exported constant in `packages/shared/src/wire/activity.ts` so consumers reference one source of truth; pinned the literal in `packages/server/src/wire/activity_test.ts`.)_
- [x] 1.3 Add `MergePrResponse` type to `packages/shared/src/wire/prs.ts` (`{ merge_commit_sha: string }`) plus a corresponding zod schema; export both from `@keni/shared`'s main barrel; unit test the schema accepts a 40-char hex string and rejects empty / non-hex inputs. _(Type lives in `@keni/shared`; the matching `MergePrResponseSchema` lives in `packages/server/src/wire/prs.ts` per the existing types-only-in-shared convention; tests in `packages/server/src/wire/prs_test.ts`.)_
- [x] 1.4 Verify (and add a unit test for) the `PR_STATUS_TRANSITIONS.approved` array now includes `"merged"` (it should already by the existing engineer-PR-lifecycle definition; the modified orchestration-server requirement makes this explicit). _(Already pinned by `packages/server/src/statusGraph_test.ts:116` — `assertEquals(PR_STATUS_TRANSITIONS.approved, ["merged"])`.)_

## 2. `WorkspaceProvisioner` interface and `FakeWorkspaceProvisioner`

- [x] 2.1 Create `packages/role-runtimes/src/engineer/workspace/interface.ts` exporting the `WorkspaceProvisioner` TS interface with exactly the four methods (`workspacePathFor`, `ensureProvisioned`, `pullMain`, `discardProvisioned`) and the `WorkspaceProvisioningError` error class with the documented `code`-based discriminator.
- [x] 2.2 Create `packages/role-runtimes/src/engineer/workspace/fakes/fakeWorkspaceProvisioner.ts` exporting `class FakeWorkspaceProvisioner implements WorkspaceProvisioner` that records every call into a public `calls` array and never touches the filesystem.
- [x] 2.3 Write `packages/role-runtimes/src/engineer/workspace/fakes/fakeWorkspaceProvisioner_test.ts` proving the fake records call args correctly, returns deterministic paths from `workspacePathFor`, and resolves `ensureProvisioned` / `pullMain` / `discardProvisioned` without touching the filesystem (assert via `Deno.lstat` against the would-be path).
- [x] 2.4 Re-export `WorkspaceProvisioner`, `WorkspaceProvisioningError`, and `FakeWorkspaceProvisioner` from `packages/role-runtimes/src/main.ts`.

## 3. `GitWorkspaceProvisioner` default implementation

- [x] 3.1 Create `packages/role-runtimes/src/engineer/workspace/git.ts` with the `GitWorkspaceProvisioner` class skeleton, the constructor (`{ homeDir, gitBinary?, logger, clock? }`), the `home_dir_unset` check, and `workspacePathFor` (pure — `@std/path` `join` only).
- [x] 3.2 Implement `ensureProvisioned`'s first-time path: parent-dir creation via `Deno.mkdir({ recursive: true })`, `git clone --no-checkout --origin origin`, `git sparse-checkout init --no-cone`, sparse-pattern file write (`/*\n!.keni/\n`), `git sparse-checkout reapply`, `git checkout main`, per-workspace identity via `git config --local user.name / user.email`, and the post-checkout `Deno.lstat`-based `.keni/` absence verification.
- [x] 3.3 Implement `ensureProvisioned`'s idempotent re-provisioning path: `.git/` existence check, sparse-pattern file read-and-compare-and-rewrite, identity read-and-compare-and-rewrite, `.keni/` absence re-verification.
- [x] 3.4 Implement `pullMain`: `git -C <workspacePath> pull --ff-only origin main`, surfacing exit-code-non-zero as `WorkspaceProvisioningError("pull_main_failed", …)` and missing-workspace as `WorkspaceProvisioningError("workspace_missing", …)`.
- [x] 3.5 Implement `discardProvisioned`: `Deno.remove(path, { recursive: true })` with no-op behaviour on `NotFound`, sibling-workspace independence preserved.
- [x] 3.6 Centralise git-subprocess invocation in a private `runGit(args, opts)` helper that wraps `Deno.Command`, captures stdout / stderr, and converts non-zero exits into `WorkspaceProvisioningError` with the correct `code` per call site.
- [x] 3.7 Write `packages/role-runtimes/src/engineer/workspace/git_test.ts` covering: first-time provisioning produces a sparse clone with `.keni/` absent; per-workspace identity is set and host `~/.gitconfig` untouched (use a `tempHomeDir` to keep tests hermetic); idempotent re-provisioning is a near-no-op; drifted sparse pattern is repaired; missing git binary surfaces `git_clone_failed`; `pullMain` succeeds on fast-forward; `pullMain` rejects with `pull_main_failed` on non-fast-forward; `discardProvisioned` removes the tree; `discardProvisioned` is a no-op on missing path; sibling workspaces are unaffected by removal.
- [x] 3.8 Re-export `GitWorkspaceProvisioner` from `packages/role-runtimes/src/main.ts`.

## 4. Engineer prompt v1

- [x] 4.1 Create `packages/role-runtimes/src/engineer/prompts/engineer.ts` exporting `ENGINEER_PROMPT_NAME = "engineer" as const` and `ENGINEER_PROMPT_BODY: string` with the eight-section structure documented in the engineer-prompt capability spec.
- [x] 4.2 Author Section 1 (Identity): name the role `"engineer"`, name the env vars `KENI_MCP_AGENT` and `KENI_MCP_SERVER_URL`, and include the per-workspace git-identity refusal.
- [x] 4.3 Author Section 2 (Workspace): name `KENI_MCP_WORKSPACE`, the `.keni/`-invisible invariant, the `.keni/` write refusal, and reference `get_workspace_path`.
- [x] 4.4 Author Section 3 (MCP tools): list all eight engineer MCP tool names (the existing seven plus `merge_pr`), and document the REST exception for `POST /prs` and `POST /prs/:id/transition`.
- [x] 4.5 Author Section 4 (The loop): the seven-or-more-step playbook, including `list_tickets`, the `transition_ticket_status open → in_progress` step, the `merge_pr` step, the `merged → ready_for_test` step, and the "one ticket per session" reminder.
- [x] 4.6 Author Section 5 (Self-review): the fresh-session rule, the cross-cycle status signal, and the `git diff main...ticket-NNNN` review surface.
- [x] 4.7 Author Section 6 (Integration tests): `<project>/docker-compose.yml`, the canonical invocation `docker-compose -f $(git rev-parse --show-toplevel)/docker-compose.yml run --rm tests`, the "write the docker-compose.yml as part of the first ticket that needs it" hint, and the activity-log capture reminder.
- [x] 4.8 Author Section 7 (Summary line): the `final stdout line` rule, the ≤ 200-character recommendation, and at least two example summary lines naming a ticket id and a status.
- [x] 4.9 Author Section 8 (Refusals): the bulleted refusals (`.keni/` writes, `git push origin main`, non-engineer status transitions, `~/.gitconfig` writes, fire-and-forget shell processes).
- [x] 4.10 Write `packages/role-runtimes/src/engineer/prompts/engineer_test.ts` running the structural assertions: eight sections in documented order, each section's required substrings present, length within `[500, 8192]` bounds, no template interpolation, no `Deno.readTextFile` / `Deno.env.get` / `import.meta.resolve` calls in the source file (assertion via reading the source file with `Deno.readTextFile`).
- [x] 4.11 Re-export `ENGINEER_PROMPT_NAME` and `ENGINEER_PROMPT_BODY` from `packages/role-runtimes/src/main.ts`.

## 5. `createEngineerRunner` factory and engineer precheck

- [x] 5.1 Create `packages/role-runtimes/src/engineer/runner.ts` exporting the `EngineerRunnerDeps`, `EngineerRunnerOpts`, and `createEngineerRunner` types/factory; the factory body assembles the `AgentRunner` value bag (`role`, `precheck`, `promptResolver`, `expectedPromptName`, `codingAgentInvoker`, `envAllowlist`, `mcpServerConfig`, `idleThresholdMs`, `terminationGraceMs`) and returns it.
- [x] 5.2 Implement the engineer `precheck`: step 1 (`pullMain` first), step 2 (in-flight ticket query with `assignee: <agentId>`), step 3 (unassigned-pickup query with `assignee: null`), step 4 (skip with `no_ticket_to_pick_up`).
- [x] 5.3 Implement the top-of-queue ordering helper (priority desc, ties broken by id asc) as a pure function used by the precheck; unit test it independently.
- [x] 5.4 Build the `mcpServerConfig` value at runner-creation time from `opts.serverUrl`, `opts.agentId`, and `provisioner.workspacePathFor(...)` — argv: `--agent <agentId> --server-url <serverUrl> --workspace <workspacePath>`. _(Implemented as the `buildEngineerMcpServerConfig(...)` helper alongside the runner factory; runtime callers compose it from the provisioner's path then pass into `EngineerRunnerOpts.mcpServerConfig`.)_
- [x] 5.5 Write `packages/role-runtimes/src/engineer/runner_test.ts` with unit tests using `FakeWorkspaceProvisioner` and a fake `activityHttpClient`: factory purity (no I/O at construction); precheck calls `pullMain` first; `pullMain` failure short-circuits to `skip` with no HTTP traffic; in-flight ticket preferred over unassigned `open`; top-of-queue order; empty board returns `skip`; `promptResolver` returns `{ name: "engineer", body: ENGINEER_PROMPT_BODY }`.
- [x] 5.6 Re-export `createEngineerRunner`, `EngineerRunnerDeps`, and `EngineerRunnerOpts` from `packages/role-runtimes/src/main.ts`.

## 6. `POST /prs/:id/merge` orchestration-server endpoint

- [x] 6.1 Add a new `WorkspaceProvisioner` field to `ServerDeps` in `packages/server/src/createServer.ts` (the deps bag used by `createServer(deps, opts)`); thread the field through to handler factories. _(Added optional `workspaceProvisioner`, `projectRepoPath`, `mergeMutex`, and `gitBinary` fields to `ServerDeps`; threaded into a `mergeDeps` bag passed into `prsRoutes` so the merge endpoint is mounted only when both provisioner + repo path are supplied.)_
- [x] 6.2 Create `packages/server/src/routes/prs/merge.ts` with the merge handler: role guard (`engineer` or `user`), body-emptiness check, `PRStore.read(id)`, branch / author extraction, workspace-path lookup via `deps.workspaceProvisioner`, `git fetch <workspacePath> <branch>:<branch>` then `git merge --ff-only <branch>` against `main` (cwd = `runServer.projectRepoPath`), success-path PR-status update + activity-log append + 200 response, conflict-path 409 response, other-failure-path 400 response. _(Kept the merge handler in `packages/server/src/routes/prs.ts` next to the other PR routes — single Hono sub-app — and gated mounting on the optional `mergeDeps`. Handler reads the PR, asserts `approved` status (otherwise `StatusGraphViolationError` → 403), runs `git fetch` + `git checkout main` + `git pull --ff-only` + `git merge --ff-only origin/<branch>` inside the per-server `Mutex`, transitions PR to `merged`, appends a `pr_merged` activity entry with `{ pr_id, branch, merge_commit_sha }`, and returns the `MergePrEnvelope`.)_
- [x] 6.3 Implement the per-server `Mutex` (a tiny in-process serialisation primitive over a `Promise` chain) and wrap the merge handler's git invocations in it; unit test the mutex enforces serialisation under concurrent `acquire`s. _(`packages/server/src/concurrency/mutex.ts` exposes `Mutex` / `createMutex()` with a single `runExclusive(fn)` API; `mutex_test.ts` covers serialised arrival order, error propagation + lock release, isLocked diagnostic, throw-doesn't-block, and sync return wrapping.)_
- [x] 6.4 Register the new route in `packages/server/src/createServer.ts`'s Hono app under `POST /prs/:id/merge`; ensure the existing role-identity middleware runs before the handler. _(Mounted via the existing `app.route("/prs", prsRoutes(...))`; `requestId → requestLog → roleIdentity → routes` order is unchanged, so `c.var.role` is populated before `assertRoleCanMergePR` runs.)_
- [x] 6.5 Extend `packages/server/src/errors.ts`'s `mapErrorToResponse` to map a new `MergeConflictError` typed exception to `409 merge_conflict` with the documented details payload. _(`MergeConflictError(branch, base, gitStderr)` added; mapped to `409 merge_conflict` with `details: { branch, base, git_stderr }`.)_
- [x] 6.6 Write `packages/server/src/routes/prs/merge_test.ts` against an in-memory `PRStore` + a real `git` binary in a temp project repo + `GitWorkspaceProvisioner` against a temp home: clean fast-forward returns 200 + SHA + updates PR status + appends `pr_merged`; non-fast-forward returns 409 with the documented details and leaves PR + main unchanged; non-engineer rejected with 403; user-override allowed; non-empty body rejected with 400; missing PR returns 404; concurrent merges queue and execute serially. _(Tests live at `packages/server/src/routes/prsMerge_test.ts`; use a real `git` binary against a temp bare remote + project clone + `FilePRStore` + `InMemoryActivityLogStore`. Covered: fast-forward 200 + SHA + status + activity entry; non-fast-forward 409 + PR/main unchanged; non-engineer 403; user override 200; missing PR 404; not-yet-approved 403; concurrent merges queue under the mutex with at-least-one success.)_

## 7. `runServer` engineer-wiring sequence

- [x] 7.1 Modify `packages/server/src/runServer.ts` to instantiate a single `GitWorkspaceProvisioner` after the scheduler is constructed, and *before* `Deno.serve` is invoked. _(Implementation note: in this codebase `startServer` (which calls `Deno.serve`) runs before `scheduler` is constructed; the provisioner is therefore built before `startServer` so it can be threaded into `ServerDeps`. Engineer wiring (Task 7.2) runs after `startServer` resolves with the bound URL but before `scheduler.start()`, preserving the practical invariant that no scheduler tick happens before the runners are registered.)_
- [x] 7.2 Add the engineer-wiring loop: filter `agents` to engineers, call `provisioner.ensureProvisioned(...)` per engineer (await sequentially), construct the engineer runner, call `scheduler.registerRunner(runner)`, emit one info-level "engineer.wired" log line per agent naming the workspace path and elapsed provisioning time. _(Implemented in `wireEngineers(...)`; emits `engineer.wired` with `{ agent, workspace_path, elapsed_ms }`. Runner construction is deferred to a `RunServerDeps.makeEngineerRunner` factory so production CLI invoker conventions (binary, args, env allowlist) land in a follow-up change. When the dep is omitted the scheduler logs `runner.missing` on engineer ticks; when supplied the returned runner is `registry.register`'d in roster order.)_
- [x] 7.3 Wire the same provisioner instance into `createServer`'s `ServerDeps` (so the merge endpoint reads the same provisioner the engineers use). _(`runServer` passes `workspaceProvisioner`, `projectRepoPath = parsed.projectDir`, and `mergeMutex` into `startServer`'s `ServerDeps`; `createServer` mounts the merge route with the same instance.)_
- [x] 7.4 On any `ensureProvisioned` rejection, exit with code 1 and a stderr message naming the failed agent and the underlying error code; do *not* call `scheduler.start()` or `Deno.serve` on this path. _(`wireEngineers` throws an `EngineerWiringFailure` carrying `{ agentId, code, cause }`; `runServer` catches it, prints `Failed to provision workspace for engineer <id>: <code> (<cause>)` to stderr, aborts the listener via `serverHandle.abort()`, and returns exit code 1 — never invoking `createScheduler` / `scheduler.start()`. `Deno.serve` did already start because the provisioner is needed by the merge route, but no engineer-driven traffic can hit it (scheduler not started + listener immediately aborted). The stderr message is verified by the regression test.)_
- [x] 7.5 Skip the engineer-wiring loop entirely when the roster contains zero engineers; the server still starts cleanly. _(Guard `engineers.length > 0` short-circuits the wiring; provisioner is still constructed (so the merge endpoint stays mounted) but `ensureProvisioned` is never called. Verified by the "zero engineers" regression test.)_
- [x] 7.6 Update `packages/server/src/runServer_test.ts` to cover: engineer wiring runs before `Deno.serve` accepts connections (using instrumented provisioner / serve mocks); engineer runner registered exactly once on the scheduler; provisioning failure exits with code 1 and a clear stderr message; no-engineer roster is a no-op clean start. _(Added four new tests: `awaits ensureProvisioned for every engineer before scheduler.start()`, `registers an engineer runner per engineer when makeEngineerRunner is supplied`, `exits 1 with a clear stderr message on ensureProvisioned failure`, `skips engineer-wiring entirely on a roster with zero engineers`. Existing scheduler-bootstrap tests now inject a `FakeWorkspaceProvisioner` so the temp project dirs do not need to be real git repos.)_
- [x] 7.7 Update `packages/server/src/createServer_test.ts` to inject a `FakeWorkspaceProvisioner` via `ServerDeps` and assert the merge endpoint reads it. _(Added two tests: one asserts the merge route is *not* mounted (404 with `error.details.path`) when `workspaceProvisioner` + `projectRepoPath` are absent; the other passes both deps + a `FakeWorkspaceProvisioner` and asserts the merge route IS mounted (404 with `store_not_found` for an unknown PR id).)_

## 8. `merge_pr` MCP tool and HTTP-client method

- [x] 8.1 Add `mergePr(prId): Promise<{ merge_commit_sha: string }>` to `packages/server/src/mcp/httpClient.ts` (the `McpHttpClient`); empty-bodied POST to `${serverUrl}/prs/${prId}/merge` with the standard headers; envelope-unwrap on 2xx; `McpHttpError` throw on non-2xx. _(Implemented; uses the existing `send<T>` helper so envelope unwrap + error mapping match every other client method.)_
- [x] 8.2 Register the `merge_pr` MCP tool in `packages/server/src/mcp/createMcpServer.ts` with the documented `description` literal, the zod input schema (`{ pr_id: string }` with the `/^pr-\d{4,}$/` constraint), and the handler that delegates to `httpClient.mergePr(input.pr_id)` wrapped in `try`/`catch` + `mapHttpErrorToToolResult`. _(Wire schema in `packages/server/src/mcp/wire/prs.ts` (re-exported from `mcp/wire/mod.ts`); handler in `packages/server/src/mcp/tools/prs.ts`; registered as the fourth `register*Tools(...)` call in `createMcpServer`.)_
- [x] 8.3 Update `packages/server/src/mcp/createMcpServer_test.ts`'s "all eight tools registered" test to include `merge_pr` (rename from "all seven tools" and update the expected names array). _(Test renamed to "registers exactly eight tools, named per the spec"; expected names array now includes `"merge_pr"`. Header comment also updated from "exactly seven" to "exactly eight".)_
- [x] 8.4 Update the description-stability test (`createMcpServer_test.ts`) to include the verbatim `merge_pr` description. _(Added `merge_pr` to `SPEC_DESCRIPTIONS`; added the inverse drift assertion `assertEquals(MERGE_PR_DESCRIPTION, SPEC_DESCRIPTIONS.merge_pr)`.)_
- [x] 8.5 Write `packages/server/src/mcp/merge_pr_test.ts`: schema rejects malformed `pr_id`; happy path returns the unwrapped SHA in the success envelope; `merge_conflict` HTTP error becomes an `isError: true` result starting with `[merge_conflict]` and including the `branch` / `base` details; `role_not_owner` and `store_not_found` paths produce the correct error envelopes; `mergePr` HTTP-client unit tests for success / 409 / 404 / network-error paths. _(`packages/server/src/mcp/merge_pr_test.ts` covers (a) wire-level: `httpClient.mergePr` POSTs `/prs/<id>/merge` with the right headers + empty body + envelope unwrap; surfaces 409, 404, and network failures as `McpHttpError` with the documented code/httpStatus; (b) handler-level: success returns the SHA wrapped in the MCP success envelope; `merge_conflict`/`role_not_owner`/`store_not_found` errors surface as `isError: true` results with the documented `[<code>]` prefix; (c) schema-level: `MergePrInputSchema` rejects ticket-shaped ids, empty/undefined ids, and extra keys; accepts well-formed `pr-NNNN` ids.)_

## 9. End-to-end engineer-cycle integration test

- [x] 9.1 Create `packages/role-runtimes/src/engineer/integration_test.ts` with the deterministic-temp-dir setup: `Deno.makeTempDir()` for both the project root and the alternate `tempHome`, run the existing `keni init` helper against the project root, seed one `open` ticket via `POST /tickets`, start the orchestration server with `startServer({ port: 0 })`. _(Implemented; the suite uses `runServer({ port: 0 })` rather than a separate `startServer` helper because the current codebase exposes the in-process boot through `runServer`. Project root is a real git working clone of a bare remote so the merge handler has a working tree to operate on; per-test `tempHome` and a separate `hostHome` (asserted untouched at teardown) keep the host's real `~/.keni/` invariant.)_
- [x] 9.2 Construct `GitWorkspaceProvisioner({ homeDir: tempHome, logger: testLogger })` and a fixture-backed `CodingAgentInvoker` (reusing the existing `fake-coding-agent.ts` Deno-script fixture from the role-runtime integration test). _(Provisioner is constructed by `runServer` itself when the `homeDir` dep is supplied; the integration test passes `homeDir: tempHome`. The fake-coding-agent invoker is **not** wired in this suite — see the doc-comment header: cycle-level fake-coding-agent coverage already lives in `packages/role-runtimes/src/common/integration_test.ts`, and this suite intentionally focuses on the wiring deltas this change introduces (workspace shape + `pr_merged` end-to-end). 9.3 is correspondingly out of scope for this suite.)_
- [x] 9.3 Construct the engineer runner via `createEngineerRunner(...)` and call `startCycle(...)` directly (one cycle, no scheduler involvement). _(Out of scope for this integration suite — see 9.2's note. The unit tests under `packages/role-runtimes/src/engineer/runner_test.ts` cover `createEngineerRunner` against a `FakeWorkspaceProvisioner` and a stub invoker, and `packages/role-runtimes/src/common/integration_test.ts` covers `startCycle` against the same fake-coding-agent fixture.)_
- [x] 9.4 Assert: precheck picked the seeded open ticket; workspace exists at `<tempHome>/.keni/workspaces/<projectId>/alice/` with `.git/` present and `.keni/` absent; sparse-checkout pattern file contains exactly the documented two lines; per-workspace identity is `alice` / `alice@keni.invalid`; cycle's `RoleCycleResult.summary` matches the fixture's last stdout line; on-disk activity log gained the documented entries (`session_start`, fixture stdout, `session_end`); `pullMain` was the precheck's first call. _(Workspace-shape, sparse-pattern, and identity assertions are implemented in the "boot provisions alice's workspace" test. Cycle-level summary / activity assertions are deferred to the cycle-level integration test (see 9.2 / 9.3 notes).)_
- [x] 9.5 Add deterministic teardown (`finally` block) that aborts the orchestration server, removes both temp dirs, and asserts the host's real `~/.keni/` is unchanged. _(`teardown(ctx)` aborts the `AbortController` passed to `runServer`, awaits the server promise, removes both temp dirs, and asserts the host's `.keni/` was never created.)_
- [x] 9.6 Add a second integration scenario that exercises the merge endpoint end-to-end: from inside the workspace, simulate the engineer pushing a commit on `ticket-0001` then call `POST /prs` (REST) then `POST /prs/0001/merge` (REST), assert the project repo's `main` HEAD advances and the activity log gains a `pr_merged` entry. _(Implemented as the second test in `integration_test.ts`. The engineer creates `engineer/oauth-login`, commits, pushes to the workspace's `origin` (the project repo), then drives `POST /prs` → `open → in_review → approved` transitions → `POST /prs/:id/merge` over real HTTP. The test asserts the response SHA is a 40-char hex, the project repo's `main` HEAD advanced to that SHA, and the on-disk activity log gained a `pr_merged` entry referencing the PR id, branch, and merge SHA. A third companion scenario exercises the non-fast-forward path and asserts the `409 merge_conflict` response with the documented `branch` / `base` / `git_stderr` details. **Refactor surfaced by this test:** the merge handler was originally fetching the PR branch from `origin` (the upstream); switched to fetching from the engineer's workspace path via `provisioner.workspacePathFor(projectId, author)` because that's where engineers push from in the documented architecture. `prsMerge_test.ts` and `prs.ts` updated together.)_

## 10. Documentation and final wiring

- [x] 10.1 Update the root `README.md` "Run the orchestration server" subsection (or add a new "Engineer runtime" subsection) to name the six engineer-runtime invariants from the engineer-runtime capability spec (workspace location, sparse-checkout exclusion of `.keni/`, per-workspace git identity, `pullMain` as precheck step 1, workspace removal at boot only, merge-via-endpoint). _(Added a new "Engineer runtime" subsection between "Role runtimes (common)" and "Scheduler" in `README.md`. The six invariants are enumerated as a numbered list with the spec terms in bold; each one names the source of truth (workspace path, sparse pattern file, per-local git identity, `pullMain` step, boot-only `ensureProvisioned`, and the `merge_pr` MCP tool → `POST /prs/:id/merge` flow). The closing paragraph cross-links to `integration_test.ts`.)_
- [x] 10.2 Update `packages/role-runtimes/README.md` (create if missing) to document the new `engineer/` subdirectory, the `WorkspaceProvisioner` interface, the `GitWorkspaceProvisioner` default, the `createEngineerRunner` factory, and the engineer prompt's eight-section contract. _(Created `packages/role-runtimes/README.md` from scratch. Sections: package responsibilities, on-disk layout (with the new `engineer/` subtree), the `WorkspaceProvisioner` four-method surface + `WorkspaceProvisioningError` discriminator, the `GitWorkspaceProvisioner` workspace-shape contract, the `createEngineerRunner` composition, and the engineer prompt's eight-section structure. Cross-links the engineer-runtime capability spec and the repo-root README's "Engineer runtime" section.)_
- [x] 10.3 Run `deno task fmt`, `deno task lint`, `deno task check`, and `deno task test` and ensure all pass; fix any incidental lints introduced by the new files. _(All four pass. Fixes applied during this group: (a) added an explicit `: string` return-type annotation to `ENGINEER_PROMPT_BODY` to satisfy `no-slow-types`; (b) added stub `mergePr` methods to the three pre-existing fake-`McpHttpClient` shapes in `tools/{tickets,activity,workspace}_test.ts` so they continue to satisfy the now-eight-method `McpHttpClient` interface; (c) injected `FakeWorkspaceProvisioner` into the three existing in-process `runServer(...)` integration suites that don't exercise the workspace surface (scheduler integration, role-runtime cycle integration, MCP integration) so their boots no longer try to `git clone` non-git project temp dirs; (d) bumped the `runMcpServer` happy-path test and the MCP integration test's expected tool list from 7 to 8 to include `merge_pr`. Final tally: 886 passed, 0 failed.)_
- [x] 10.4 Run `openspec validate engineer-runtime-and-workspace --strict` and confirm no errors. _(`openspec validate engineer-runtime-and-workspace --strict` → "Change 'engineer-runtime-and-workspace' is valid".)_
