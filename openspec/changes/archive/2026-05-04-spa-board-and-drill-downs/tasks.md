## 1. apiClient additions (transport seam)

- [x] 1.1 Extend `packages/spa/src/transport/apiClient.ts`'s `ApiClient` interface with the nine new methods (`getTicket`, `createTicket`, `patchTicket`, `transitionTicket`, `getPr`, `patchPrIntent`, `transitionPr`, `mergePr`, `appendActivity`). Each method's return type imports from `@keni/shared` (`TicketEnvelope`, `PREnvelope`, `MergePrEnvelope`, `ActivityEnvelope`). Each method's parameter types import from `@keni/shared` too (`TicketCreateRequest`, `TicketHeaderPatchRequest`, `TicketTransitionRequest`, `PRIntentPatchRequest`, `PRTransitionRequest`, `ActivityAppendRequest`).
- [x] 1.2 Implement the nine methods in the `createApiClient` factory, mirroring the existing methods' shape: build a `Request` with the `X-Keni-Role` header (default `user`), call `fetch`, parse JSON, branch on `response.ok`, throw `KeniApiError(status, body.error.code, body.error.details)` on non-2xx.
- [x] 1.3 Implement query-string serialisation for `filter?: { ticket?: string }` on `listPrs` (the existing `listPrs(filter?)` already exists but may not yet serialise `ticket`) — verify the existing `server` routes `GET /prs?ticket=<id>` and add the `ticket` field to the SPA-side filter type if missing.
- [x] 1.4 Extend `packages/spa/src/transport/apiClient_test.ts` with new `Deno.test` cases: (a) `getTicket` happy path and 404 → `KeniApiError("store_not_found")`; (b) `createTicket` sends the typed body and returns the envelope; (c) `patchTicket` sends the typed patch and surfaces `status_in_patch` / `validation_failed`; (d) `transitionTicket` sends `{from, to}` and surfaces `status_graph_violation` / `role_not_owner` / `stale_state`; (e) `getPr` / `patchPrIntent` / `transitionPr` / `mergePr` happy paths and error mappings (including `merge_conflict`); (f) `appendActivity` sends the typed body, including `refs: { ticket }`, and returns the envelope.
- [x] 1.5 Verify: `deno test -A packages/spa/src/transport/apiClient_test.ts` exits 0 with all new and existing cases passing.
- [x] 1.6 Static-grep check: confirm no file outside `apiClient.ts` / `apiClient_test.ts` calls `fetch(` against an orchestration-server path (enforces the `spa-shell` capability's existing requirement).

## 2. Status-graph mirror and drift-check

- [x] 2.1 Create `packages/spa/src/features/shared/statusGraph.ts` exporting `SPA_TICKET_STATUS_TRANSITIONS` and `SPA_PR_STATUS_TRANSITIONS` as frozen `Readonly<Record<...>>` constants. Values verbatim per `spec.md` §4.1 for tickets and the orchestration-server spec's PR graph for PRs. Include a top-of-file comment cross-linking the orchestration-server spec's "A status-graph constant encodes the §4.1 ticket lifecycle" requirement.
- [x] 2.2 Create `packages/spa/src/features/shared/statusGraph_test.ts` importing the server's `TICKET_STATUS_TRANSITIONS` and `PR_STATUS_TRANSITIONS` (from `@keni/server` — the workspace member allows cross-package imports at test time; if the import path is not yet re-exported from the server's `mod.ts`, add the `export { TICKET_STATUS_TRANSITIONS, PR_STATUS_TRANSITIONS } from "./transitions.ts"` line in the server's public module). Assert deep equality with the SPA mirror.
- [x] 2.3 Verify: `deno task check` from the workspace root passes; `deno test -A packages/spa/src/features/shared/statusGraph_test.ts` exits 0.

## 3. Board view (`spa-board`)

- [x] 3.1 Create `packages/spa/src/features/board/BoardCard.tsx` exporting a default `<BoardCard ticket: TicketSummaryResponse />` component. Render id (monospace), title, assignee (or `—`), priority (badge). Wrap the outermost element in `<Link to={\`/tickets/${id}\`}>` from `react-router-dom`. Attach an `onDragStart` handler that writes `{ ticketId, fromStatus }` into `event.dataTransfer` as a JSON string. Set `draggable="true"`.
- [x] 3.2 Create `packages/spa/src/features/board/BoardCard.css` using `--keni-*` tokens (card padding, border, priority-badge color based on range, `data-error` styling).
- [x] 3.3 Create `packages/spa/src/features/board/BoardColumn.tsx` exporting a default `<BoardColumn status: TicketStatus; tickets: TicketSummaryResponse[]; onDrop: (from, to, ticketId) => void />` component. Render the column header (Title Case, count in parentheses), set `data-status` to the status value, render each ticket as a `<BoardCard>`. Attach `onDragOver` (calls `event.preventDefault()`, sets `data-drop-target="true"`), `onDragLeave` (clears `data-drop-target`), `onDrop` (reads `dataTransfer`, calls `onDrop(from, to, ticketId)`, clears `data-drop-target`).
- [x] 3.4 Create `packages/spa/src/features/board/CreateTicketForm.tsx` — collapsible inline form with `title` (required, non-empty), `priority` (required, `type="number" step="1"`, default 100), optional `assignee`, optional `change_request`, optional `body` textarea. Submit calls `apiClient.createTicket(input)`, disables the submit button during the in-flight call, navigates to `/tickets/<new-id>` via `useNavigate()` on success, renders an inline error below submit on failure.
- [x] 3.5 Create `packages/spa/src/features/board/BoardView.tsx` — the view container. States: `tickets: TicketSummaryResponse[] | null` (null = loading), `error: KeniApiError | null`, `disconnected: boolean`, per-card `cardErrors: Record<TicketId, string | null>`. Effects: (1) on mount, call `listTickets()`; (2) subscribe to `eventsClient` for frames (`ticket.created` / `ticket.updated`) and lifecycle events. Frame handlers per `spa-board` capability — `ticket.created` → `getTicket(payload.ticket_id)` → append; `ticket.updated` with `kind: "transition"` → in-place status update (no REST); `ticket.updated` with `kind: "patch"` → `getTicket` + replace. Lifecycle handler: on `"connected"` refetch via `listTickets()`; on `"disconnected"` set `disconnected: true`. Render branches: loading (`tickets === null && error === null`), error (`error !== null`), populated (always renders the twelve columns plus `<CreateTicketForm>` when `tickets !== null`). Define `onDrop(from, to, ticketId)`: if `from === to` no-op, else call `apiClient.transitionTicket(ticketId, { from, to })`; on success update local state; on failure set `cardErrors[ticketId] = err.code`.
- [x] 3.6 Create `packages/spa/src/features/board/BoardView.css` with the flexbox row layout (`overflow-x: auto`, `gap: var(--keni-space-3)`, column `min-width: 240px`), the `[data-disconnected="true"]` dim rule, the loading / error state layouts, and the form toggle's collapsed / expanded states.
- [x] 3.7 Create `packages/spa/src/features/board/dragHelpers.ts` — pure helpers `packDragPayload({ticketId, fromStatus})` and `unpackDragPayload(dataTransfer: DataTransfer)` that marshal the DnD payload via `dataTransfer.setData("application/keni-ticket-drag", JSON.stringify(...))` / `dataTransfer.getData(...)`. Include a companion unit test `dragHelpers_test.ts` covering round-trip and malformed-payload cases.
- [x] 3.8 Create `packages/spa/src/features/board/BoardView_test.tsx` (import `../../test_setup.ts` first). Nine `Deno.test` cases per the `spa-board` capability: loading / empty / error / disconnected; twelve columns in order; `ticket.created` frame appends after refetch; `ticket.updated` kind transition moves in place; `ticket.updated` kind patch refetches; successful drop calls `transitionTicket` and moves the card; failed drop leaves card in place with `data-error`; create-ticket navigates on success; `"connected"` lifecycle triggers refetch.
- [x] 3.9 Verify: `deno test -A packages/spa/src/features/board/` exits 0 with all cases passing.

## 4. Ticket detail view (`spa-ticket-detail`)

- [x] 4.1 Create `packages/spa/src/features/ticketDetail/TicketHeaderEdit.tsx` — inline-editable title with click-to-edit, Enter / blur commits, Escape cancels. Props `{ title: string; onCommit: (next: string) => Promise<void>; error: string | null }`. While `onCommit` is pending, disable the input; on error (set externally via `error` prop), re-enable and render the error next to the input.
- [x] 4.2 Create `packages/spa/src/features/ticketDetail/TicketBodyEdit.tsx` — body textarea with Edit / Save / Cancel. Props `{ body: string; onCommit: (next: string) => Promise<void>; error: string | null }`.
- [x] 4.3 Create `packages/spa/src/features/ticketDetail/TicketTransitionPanel.tsx` — `<details>` labelled `Advanced: transition (prototype only)`. Read `SPA_TICKET_STATUS_TRANSITIONS[from]` for the `to` dropdown options. On Transition click call `apiClient.transitionTicket(id, { from, to })`; surface errors below the button. Render the documented caveat text verbatim. When the `to` options list is empty, render the disabled placeholder.
- [x] 4.4 Create `packages/spa/src/features/ticketDetail/TicketCommentThread.tsx` — renders the filtered comment rows (one row per `refs.ticket === id && event === "ticket_comment"` activity entry, newest-first? No — the capability says chronological oldest-first) plus the "Post comment" form (textarea with character counter `<used>/3800`, Post button disabled when empty or > 3800, submit calls `apiClient.appendActivity(...)` with the documented payload).
- [x] 4.5 Create `packages/spa/src/features/ticketDetail/useTicketActivity.ts` — a small hook that manages the activity list state, the debounced refetch on `activity.appended` (with `TICKET_ACTIVITY_REFETCH_DEBOUNCE_MS = 250`), the `"connected"` lifecycle refetch, and the unmount cleanup. Returns `{ activity, refetch, loading, error }`.
- [x] 4.6 Create `packages/spa/src/features/ticketDetail/TicketDetailView.tsx` — the view container. Reads `id` from `useParams()`. States: `ticket: TicketResponse | null`, `ticketError: KeniApiError | null`, `titleError: string | null`, `bodyError: string | null`, `transitionError: string | null`, `linkedPrs: PRSummaryResponse[]`, `disconnected: boolean`. Effects: (1) on mount / `id` change, call `getTicket(id)` and `listPrs({ ticket: id })`; (2) use `useTicketActivity(id, ...)` to drive the activity list; (3) subscribe to `eventsClient` for `ticket.updated` (matching `id` → refetch) and `pr.created` / `pr.updated` (matching `ticket: id` or local PR ids → refetch `listPrs`); (4) on `"connected"` lifecycle refetch everything; (5) on unmount unsubscribe. Render: header (id, title, status pill, metadata row with assignee / priority / change_request), body, status history section (activity filtered by `refs.ticket === id`), comment thread, transition panel, linked PRs, loading / error / not-found / disconnected states.
- [x] 4.7 Create `packages/spa/src/features/ticketDetail/TicketDetailView.css` with the detail layout (header + main content grid), the status pill colors (one per status via `--keni-color-status-*`), comment row styling, and transition-panel layout.
- [x] 4.8 Create `packages/spa/src/features/ticketDetail/TicketDetailView_test.tsx` (import `../../test_setup.ts` first) with the nine documented test cases per the `spa-ticket-detail` capability.
- [x] 4.9 Verify: `deno test -A packages/spa/src/features/ticketDetail/` exits 0 with all cases passing.

## 5. PR detail view (`spa-pr-detail`)

- [x] 5.1 Create `packages/spa/src/features/prDetail/PRIntentEdit.tsx` — body editor with Edit / Save / Cancel. Props `{ intent: string; onCommit: (next: string) => Promise<void>; error: string | null }`.
- [x] 5.2 Create `packages/spa/src/features/prDetail/PRTransitionPanel.tsx` — `<details>` labelled `Advanced: transition (prototype only)`. Read `SPA_PR_STATUS_TRANSITIONS[from]`. On Transition click call `apiClient.transitionPr(id, { from, to })`. Render the caveat text verbatim. Handle the empty-options case.
- [x] 5.3 Create `packages/spa/src/features/prDetail/PRMergeButton.tsx` — rendered only when `status === "approved"`. On click, show `window.confirm(...)`; if confirmed, disable the button and call `apiClient.mergePr(id)`. Handle success (view-level refetch via the callback prop `onMerged`), handle `merge_conflict` (render a prominent error panel via another callback prop), handle other errors (inline).
- [x] 5.4 Create `packages/spa/src/features/prDetail/PRDetailView.tsx` — the view container. Reads `id` from `useParams()`. States: `pr: PRResponse | null`, `prError: KeniApiError | null`, `intentError: string | null`, `transitionError: string | null`, `mergeConflict: { message: string; details: unknown } | null`, `disconnected: boolean`. Effects: (1) on mount / `id` change, call `getPr(id)`; (2) subscribe to `eventsClient` for `pr.updated` (matching `id` → refetch) and lifecycle events; (3) on `"connected"` refetch; (4) on unmount unsubscribe. Render: header (id, title, status pill, Merge button when approved), metadata (Linked ticket link, branch, author, timestamps), intent block, transition panel, loading / error / not-found / disconnected states.
- [x] 5.5 Create `packages/spa/src/features/prDetail/PRDetailView.css` following the same token conventions as the ticket detail view.
- [x] 5.6 Create `packages/spa/src/features/prDetail/PRDetailView_test.tsx` with the six documented test cases per the `spa-pr-detail` capability. Tests that stub `window.confirm` SHALL do so on `globalThis.confirm` and restore it in the `finally` block (or via a test helper).
- [x] 5.7 Verify: `deno test -A packages/spa/src/features/prDetail/` exits 0 with all cases passing.

## 6. Activity log view (`spa-activity-log`)

- [x] 6.1 Create `packages/spa/src/features/activityLog/formatActivityRefs.tsx` exporting `<ActivityRefs refs: Readonly<Record<string, string>> />`. Render `ticket` → `<Link to={\`/tickets/${v}\`}>`, `pr` → `<Link to={\`/prs/${v}\`}>`, `change_request` → plain `<span>`, other keys → plain `<span>`. Include a companion unit test `formatActivityRefs_test.tsx` covering each of the three documented keys plus a non-documented key.
- [x] 6.2 Create `packages/spa/src/features/activityLog/ActivityEntryRow.tsx` — one row per `ActivityEntryResponse`. Render timestamp (via `formatRelativeTime`, with the exact ISO as `title` attr), agent (monospace), role, event (bold), summary (or `—`), refs via `<ActivityRefs>`.
- [x] 6.3 Create `packages/spa/src/features/activityLog/ActivityFilters.tsx` — the filter form. Inputs: `agent` (text), `role` (dropdown with `<any>` + 5 roles), `from` (`datetime-local`), `to` (`datetime-local`). A `Clear filters` button. Debounced (`ACTIVITY_FILTER_DEBOUNCE_MS = 250`) via `useEffect` + `setTimeout`. Emits filter changes through an `onFilterChange(filter: ActivityFilter)` prop.
- [x] 6.4 Create `packages/spa/src/features/activityLog/ActivityLogView.tsx` — the view container. States: `filter: ActivityFilter`, `entries: ActivityEntryResponse[] | null` (null = loading), `error: KeniApiError | null`, `disconnected: boolean`. Effects: (1) on mount and on `filter` change (after debounce), call `listActivity(filter)`; (2) subscribe to `eventsClient` for `activity.appended` frames (filtered match → debounced refetch) and lifecycle events; (3) on `"connected"` refetch. Render the entries in reverse-chronological order (reverse the id-ordered array). Handle loading / error / empty / disconnected.
- [x] 6.5 Create `packages/spa/src/features/activityLog/ActivityLogView.css` using the `--keni-*` tokens.
- [x] 6.6 Create `packages/spa/src/features/activityLog/ActivityLogView_test.tsx` with the seven documented test cases per the `spa-activity-log` capability.
- [x] 6.7 Verify: `deno test -A packages/spa/src/features/activityLog/` exits 0 with all cases passing.

## 7. Retire placeholders and wire real routes

- [x] 7.1 Delete `packages/spa/src/shell/BoardPlaceholder.tsx`.
- [x] 7.2 Delete `packages/spa/src/routes/RoutePlaceholder.tsx`.
- [x] 7.3 Update `packages/spa/src/App.tsx`'s route registry: `/` → `<BoardView />`; `/tickets/:id` → `<TicketDetailView />`; `/prs/:id` → `<PRDetailView />`; `/activity` → `<ActivityLogView />`; catch-all unchanged.
- [x] 7.4 Update `packages/spa/src/shell/AppShell_test.tsx` (or the routing-section thereof — may have been split to `App_test.tsx` in step 10; follow whichever convention the repo currently uses): replace the `<BoardPlaceholder />` assertion at `/` and the `data-testid="route-placeholder"` assertions at `/tickets/:id` / `/prs/:id` / `/activity` with assertions that the real view components mount (e.g., `screen.getByText(...)` for each view's documented header text, or the view-specific `data-testid` elements like `data-testid="board-loading"`, `data-testid="ticket-loading"`, etc. — these are reliable because the test harness mounts against in-memory clients with pending promises).
- [x] 7.5 Search `packages/spa/src/` for any remaining references to `BoardPlaceholder` or `RoutePlaceholder` and remove them. Confirm `deno task check` is green.
- [x] 7.6 Verify: `deno test -A packages/spa/` exits 0 — the retired placeholder tests are replaced by assertions on the real components.

## 8. Design tokens and global styling additions

- [x] 8.1 Extend `packages/spa/src/theme/tokens.css` with board-specific tokens: `--keni-color-status-open`, `--keni-color-status-in-progress`, `--keni-color-status-ready-for-review`, `--keni-color-status-in-review`, `--keni-color-status-has-comments`, `--keni-color-status-approved`, `--keni-color-status-merged`, `--keni-color-status-ready-for-test`, `--keni-color-status-in-testing`, `--keni-color-status-tested`, `--keni-color-status-test-failed`, `--keni-color-status-done`, plus `--keni-board-card-padding` and `--keni-board-column-min-width` (240px). Add dark-mode overrides in the `@media (prefers-color-scheme: dark)` block for every color token.
- [x] 8.2 Pick color values that pass WCAG AA contrast for the status pill's foreground-on-background combination in both themes; document inline.
- [x] 8.3 Verify: `deno task dev` renders the board against a seeded in-memory backend (see step 10 task list for the dev-server proxy convention) and the status pills are visually distinguishable in both light and dark themes.

## 9. Wire-shape barrel sweep

- [x] 9.1 Confirm `packages/shared/src/wire/mod.ts` already re-exports every type this change imports. Expected imports: `TicketCreateRequest`, `TicketHeaderPatchRequest`, `TicketTransitionRequest`, `TicketEnvelope`, `TicketResponse`, `TicketSummaryResponse`, `TicketListResponse`, `PRCreateRequest` (unused in SPA but already exported — no action), `PRIntentPatchRequest`, `PRTransitionRequest`, `PREnvelope`, `PRResponse`, `PRSummaryResponse`, `PRListResponse`, `MergePrEnvelope`, `MergePrResponse`, `ActivityAppendRequest`, `ActivityEnvelope`, `ActivityEntryResponse`, `ActivityQueryResponse`, `EventFrame`, `EventName`, all six payload interfaces (including `TicketCreatedPayload`, `TicketUpdatedPayload`, `PRCreatedPayload`, `PRUpdatedPayload`, `ActivityAppendedPayload`), `ErrorResponse`, `ErrorCode`, `Role`. Add any missing `export type { ... } from "./..."` lines in alphabetical order.
- [x] 9.2 Drift check (manual): temporarily add a required field to `TicketResponse` and confirm `deno task check` fails pointing at `TicketDetailView.tsx` (the destructure / render missing the field) or at `apiClient.ts`'s `getTicket` return type. Revert.

## 10. Integration check and polish

- [x] 10.1 Workspace-level `deno task check` exits 0.
- [x] 10.2 Workspace-level `deno task test` exits 0 — every new `*_test.tsx` file is discovered and all cases pass.
- [x] 10.3 Workspace-level `deno task lint` and `deno task fmt --check` exit 0.
- [x] 10.4 Workspace-level `deno task build` exits 0 and `packages/spa/dist/` contains the updated production bundle.
- [ ] 10.5 Smoke-test end-to-end: run `deno task -f @keni/server start` against a `keni init`-produced temp dir; in another shell run `KENI_SERVER_URL=http://127.0.0.1:<port> deno task -f @keni/spa dev`; open the browser; confirm (a) the board renders the twelve columns; (b) creating a ticket appears in the `open` column within one round-trip; (c) dragging the card to `in_progress` moves it and persists (a reload shows it in the new column); (d) opening the ticket detail shows the ticket and an empty comment thread; (e) posting a comment appears after a brief refetch; (f) opening `/activity` shows the expected activity entries with the comment and the transition; (g) dropping a card on an invalid column surfaces a `status_graph_violation` error on the card; (h) toggling the orchestration server off and on produces `disconnected` → `connecting` → `connected` indicators and the views refetch on `connected`.
- [x] 10.6 Run `openspec validate spa-board-and-drill-downs` and confirm the change validates.

## 11. Documentation and capability archiving

- [x] 11.1 Update the root README's "Run the SPA" subsection (if any narrative change is needed) to name the four view routes and the drag-and-drop transition affordance. Cross-link the four new capability specs.
- [x] 11.2 Ensure the `openspec/changes/spa-board-and-drill-downs/specs/` directory contains exactly five spec files (`spa-board/spec.md`, `spa-ticket-detail/spec.md`, `spa-pr-detail/spec.md`, `spa-activity-log/spec.md`, `spa-shell/spec.md`).
- [x] 11.3 On merge, run the archive workflow (`openspec archive spa-board-and-drill-downs`) which moves the change into `openspec/changes/archive/<yyyy-mm-dd>-spa-board-and-drill-downs/`, applies the `spa-shell` delta to `openspec/specs/spa-shell/spec.md`, and creates the four new `openspec/specs/spa-*/spec.md` files.
